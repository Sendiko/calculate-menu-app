package com.sendiko.calcmenus.ui.screens.employee.menu_screen

object MenuTypeList {
    val menuTypeList = listOf(
        MenuType(
            type = "Food",
            isSelected = true
        ),
        MenuType(
            type = "Beverage",
            isSelected = false
        )
    )
}