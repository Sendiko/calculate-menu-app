package com.sendiko.calcmenus.employee.menu.presentation

object MenuTypeList {
    val menuTypeList = listOf(
        MenuType(
            type = "Food",
            isSelected = true
        ),
        MenuType(
            type = "Beverage",
            isSelected = false
        )
    )
}